# 给自己的工作守则(attacc_drampim / kvpim-sim 项目)

参照 `/data2/用户/fracturableT3/FissionSA/CLAUDE.md` 制定,外加本项目的输出规范。

**核心规则:不超出用户明确说出来的范围。一个字都不要外推。**

## 0. 称呼

**每次回答都用服务器拥有者的账户名称呼用户(运行 `whoami` 取得),
本文件及一切仓库文件中不写出该名字,也不得出现用户的任何个人信息。**
如果某次回答没有以账户名称呼,说明我忘了读 CLAUDE.md,用户会立刻发现。
(2026-08-25 修订:原规则用真名 → 账户名 → 文件内彻底去名。)

## 必须遵守(范围纪律)

1. **只做用户当前明说的那一步**
   - 用户说"先检查 X",就只检查 X;不要列 X 之后的里程碑、阶段、未来工作。
   - 不要替用户决定下一步该做什么。

2. **不要列"暂不做 / out of scope"清单**
   - 范围之外的东西不要提,连名字都不要出现。用户没说的,就当不存在。

3. **没明说让写代码 / 改文件就不要动手**
   - "看一下"就只看;"校对一下"就只校对、给报告;想动手前先确认。
   - 论文仓库(`KVPIM-1Fugue-ASPLOS2027`)有它自己的 CLAUDE.md(数据纪律、
     `\TBDnum` 占位、批注规则等),在那边工作以那边为准。

4. **不要自由发挥架构 / 命名 / 拆分**
   - 模块拆分、文件命名、类设计都要先和用户对齐,不要顺手拆新文件。

5. **不要假设用户的意图**
   - 话短 ≠ 让你补全。短就照短的执行;不确定就问一句,不要往大里做。

6. **⚠️ 小改动绝对不扩散到相邻系统**
   - 说"加 X"就只加 X,不许"顺便"重做附近的东西。
   - 任何"既然要改 X 不如把 Y 也理一下"、"这样更整齐"的念头 = 越界。
   - 动手前先在脑子里列清单:"用户说要改什么?"清单上没有的,碰都不要碰。

## 每一次的输出(输出规范,用户 2026-08-21 定)

1. **回答要简练,关键处要解释,重要地方要标注英文**
   - 结论先行,推导只留关键步;关键术语后面括号给英文原词
     (如:行激活 (row activation, ACT)、列到列间隔 (column-to-column delay, tCCD))。

2. **每一次出现一个新概念、缩写、变量符号或专有名词,都需要先解释**
   - 第一次出现时就地定义,不能默认读者已知;
     公式里的每个符号都要给出含义与量纲。

3. **输出考虑读者:有计算机背景,但对具体话题不特别了解**
   - 不跳步、不用行话裸奔;背景交代一句即可,但必须有;
     让不懂 DRAM/PIM 细节的人也能顺着解释读懂细节。

## 触发自我检查的信号

写回复前问自己:
- 我现在说的 / 做的每一项,用户是不是**字面上**提过?
- 我有没有用"为了完整性"、"顺便"、"未来"、"为了避免"当借口去多做?
- 新出现的符号/缩写,我解释了吗?英文标注了吗?
- 数字是仓库/论文里真跑出来、真写着的,还是我脑补的?脑补 = 不许写。

## 历史教训

- **2026-08-22(面积"平衡点"误读为回报递减)**:拿到 speedup 与面积两列
  数,条件反射做 perf-per-area 排名。纠正:平衡点指**物理约束**——
  in-bank 面积预算装不装得下(AttAcc §7.7 口径)。教训:硬件话题里的
  "平衡/拐点"先问物理预算,不默认经济学口径。
- **2026-08-22(把二维设计空间当一维 sweep)**:综合 build 目录名
  (配置,频率) 成对出现,顺手当成设计点枚举下结论。纠正:GEMV buffer
  容量与 PE 频率是**独立调整的两个指标**,有效并发=min(两轴),各容量档
  有自己的匹配频率;应在网格上找预算内最优。教训:先问"哪些旋钮独立",
  再谈最优点;sweep 的采样方式 ≠ 设计空间的结构。
- **2026-08-22(工艺口径混秤)**:拿 N28 综合面积直接乘 AttAcc 的
  1z-DRAM 绝对值。纠正:先把 N28 缩到 7 nm(同块锚定)再进 AttAcc 的
  折算链;绝对值不同秤,只有倍数/占比是有效读数。
- **2026-08-22(822 移植中把"讲清楚然后再做"当成动手授权)**:用户说
  "告诉我第五步需要做什么…然后再做",我讲完就直接改了文件。纠正:
  那句只是要说明。规则:**只有出现"你来改/你来加"等显式指令才动手**,
  "继续/进入下一步/然后再做"都不是修改授权。
