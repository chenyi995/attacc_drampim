# 2026-08-27:head→HBM 重映射日(chenyi9 裁决记录)

## 发现:head↔channel 映射不自洽(R18;全文 `../README_head_hbm_remap.md`)

- 起点是 chenyi9 连环追问("一个 head 一个 hbm 下面的 chunk 才能分
  开"、"256 token 是多少 bank 的一个 dram row?"、"一个 channel 到底
  多少 bank?"),把 trace 层与 TLB 层对同一批 channel 的两套解释顶
  了出来:
  - **trace 层**(AttAcc 上游语义):一个 head 一个 **channel**,
    `lch * HBM_GS['ch']` 逐 channel 复制同一列样式;
  - **TLB 层**(本仓布局):token 块按 channel **条带化**轮转。
- 7B/TP8 退化定量:`numOp=4`、`num_hbm=5` →
  `n_head_per_hbm = ceil(4/5) = 1` → legacy 路 `valid_channel=1`
  (单 channel 扫全 L,15/16 channel 闲置;实测 L=256 的 64 条
  MAC_AB 全落 channel 0);池路 15 channel 各复制同列 = 15 个幻影
  head,能耗虚增 ~15×。
- **归因(git 考古,chenyi9 指令"这个错误是哪个 commit 留下来的?
  谁的 commit?")**:三层叠加——放置规则本体 `c1540de`(jwchoi,
  AttAcc 上游 initial commit,2024-05-06,对上游自身配置自洽);带进
  池化/复用路径 `47ae0c3`(xw338,2026-08-21,`ch_delta`/`pool_base`
  保留 lch=head 复制语义);退化配置下出数未察觉 `fb9fabe` 起本线
  (chenyi9)实验链(2026-08-22 起)。

## 修复:head→HBM 条带重映射(裁决:chenyi9,"按照 head 一个 hbm
的重映射改好,先改好,再跑")

- 规范:一个 head 住一个 HBM;run 的各 channel 载该 head **自己的
  token 条带**,`stripe_width = channels // heads_per_hbm`、
  `L_per_channel = ceil(L/stripe_width)`、`num_itr = 1`;head 并行 =
  trace 外 HBM 并发(通式 `heads_per_hbm = ceil(local_heads/num_hbm)`,
  本配置 = 1)。chunk 几何不变(256 token = 64 bank × 1 row,
  32+32 MAC_AB),只是摊上 16 channel(每 channel 串行深度 64→4)。
- 改动:trace 生成器加 `--head-hbm-stripe`(默认关,旧语义逐字节保
  留);wrapper 恒传该旗标、签名加版本记号 `hbmstripe1`、缓存轮换
  `signature_cache_v2_headhbm.jsonl`(旧 ~75 万行留盘归档不载入);
  workload_runner 落 KV 事件改 **per-HBM 带宽份额**、D_i 位图按
  **广播**计(die 侧 q/tuple 聚合/聚合自洽,不动)。
- 实现教训:第一版把缩 L 放 `Attention()` 内部,拼装环仍用原 L 推
  `length` → 大 L IndexError;改为 `run_attention` 顶部缩,生成与
  拼装同一个 L。
- 验证:trace 对照(旧 64 MAC_AB@ch0 → 新 4×16,总量守恒);
  L∈{256,1024,8192,30000}/池/MQ/相位全过;单测 41/41
  (测试桩 PIM 无 `num_hbm`,记账处 `getattr` 兜底)。
- **影响:2026-08-27 之前全部 PIM 侧输出作废**(fig:motiv 套件、
  wl_tiny 阶梯、star r3 全表等);GPU/链路侧不受影响。

## 重跑(chenyi9:"做完就开始跑我们的任务")

- wl_tiny 冒烟(A1/A4/A5,dag)通过后,重启
  `run_dag_suite.sh LLAMA-7B`(5 workload × k∈{2,4,8,16,32},
  N_PAR=3、RAMU_WORKERS=4,96 核预算);v2 签名缓存从零暖起,
  首轮 ETA 较暖缓存版拉长。

## 午后:逐 tier 三指标出数(裁决 chenyi9)

- **裁决**:"如果是多个 tier 的,不能给一个 e2e"——随共享池逐 tier 累积
  (star:3k → 16.5k token),每个 tier 出**三个指标 prefill / decode /
  e2e,任一指标把某档与其他档拉开即可读**("一个 tier 的三者其中之一")。
- **实现(只改收集器,引擎与报告格式不动)**:decode 批记录本就带
  `tier` 字段与 DAG 时间戳,`collect_dag_ladder.py` 新增
  `dag_ladder_tiers.csv`:`prefill_s`(上一 tier 末次 decode 戳 →
  本 tier 首个 Q 到达)、`decode_s`(本 tier 批跨度)、
  `tier_total_s`/`cum_end_s`(e2e 与台阶曲线)、`decode_batches`。
  冒烟验证:A1 tier-0 prefill 0.553 s vs A4 0.076 s(×7.2),同 tier
  decode 均 0.38 s——"prefill 拉开、decode 重合"按预期可读。
  待验证:A2(纯 GPU 路)报告是否含批记录,首批真实 CSV 落地时核对。
- **纸面分离地图**(star,k=8,节拍×命令数推算;GPU 权重底
  15.7 µs/层/步):A1 靠 **prefill** 轴(tier 1 起 ×6 → ×105);A2 靠
  **decode** 轴全程(×5 → ×200,∝ 上下文);A3 vs A3a 断流轴 tier 1 起
  ×2.3 → ×30;A3a/A4 vs A6 (replicate vs MQ) tier 3 起 ×1.9 → ×4;
  A5 只在 fresh 重的主 agent tier 靠 prefill 归边与 A6 区分;A6 自身
  tier 7 起露出地板(×1.5–2.2)。主 agent tier(波宽 1)各 PIM 档
  decode 重合是结构性对照,不是测量失败。
- 同日纸面 energy 结论(记录备查):能量记账 = 流量 × 每字节表,断流
  只加时间不加能量 → **A3 时间惨、能量不惨**;能量主分界是列读复制
  (replicate ×8 vs MQ ×1,star 规模差 ~30%,wl_tiny 上只有 0.5%——
  MQ 能量收益随规模增长);A1/A2 在此之上再背全量重算 / 链路搬运
  (×1.6–2.1)。

## 午后:RAM 治理(裁决 chenyi9)

- **事件**:N_PAR=3(3 批 × 7 档 = 21 个构图进程,单进程 27–58 GB)把
  754 GB 机器压到 746 GB used + 67 GB swap,套件在 swap 抖动中爬行。
- **裁决序列**:限 600 GB → 收紧到 500 GB → 最终**允许 560 GB、
  N_PAR=2**。对应动作:杀套件 → N_PAR=2(600 口径)→ N_PAR=1 +
  RAMU_WORKERS=14(500 口径)→ **N_PAR=2 + RAMU_WORKERS=6**
  (560 口径;2×6×6+14 = 86 ≤ 96 核)。重启损失有限:已仿真签名都在
  v2 磁盘缓存,重启即命中。
- **哨兵**:内存监视 60 s 一查,≥520 GB 告警(允许线 560);套件驱动
  日志 `output/suite_driver_20260827-132112_npar2.log`,13:21 起
  star+pipeline k=2 两批在跑。

## 傍晚:阶梯差距连环追问与选边代码审计(chenyi9 提问驱动)

chenyi9 就"A3/A3a 与 A6 差多少"连环追问五轮,三处纠正了我的纸面
推算,最终落到一次代码审计。逐轮记录:

1. **"A3a/A3 与 A6 相差多少"**:逐 tier 加权重算(star,真实 ctx/
   波宽/decode 量)→ decode 轴 A3a/A6 ≈ ×2.7(深 tier 渐近 ×3.9),
   A3/A6 ≈ ×4.5/×9/×30(k=2/8/32);e2e A3a ≈ ×2.0–2.6。
2. **"为什么 prefill 轴分不开?A3 的 prefill attention 在 GPU 啊"**
   ——chenyi9 对,我漏了**读回项**:A3/A3a/A4 的 GPU prefill 要把
   整段驻留 KV 拖回(∝ ctx,与 k 无关;`prefill_kv_readback`),
   A5/A6 PIM 侧免读回。prefill 指标 A3≈A3a≈A4 一伙对 A5/A6 拉开
   ×2–10(随 ctx),A3 与 A3a 之间仍只靠 decode 断流轴分。
3. **"MQ 8 个 batch 不是 6 tCK 吧"**——chenyi9 对:C 模型
   `ceil(8/(1.733×0.769)) = ceil(6.0027) = 7 tCK`,预设 1.733 GHz 比
   真配平点 1.7337 差 0.04%,取整罚一整拍(+17% MAC 节拍;A6 扫描
   总量 +5%,MQ 大头是每查询 MV_SB/MV_GB 搬运)。想踩 6 拍线预设
   应为 1.734 GHz——仅记录,未改。
4. **"重算增加 PIM 计算量也会增加;全量重算时 A6 必选 GPU"**——
   chenyi9 对:bank-whole prefill 每 8 个查询一趟全域扫,
   sweeps = ceil(m/8) 随修正行数线性;m 大到阈值 A6 翻 GPU,与
   A3a 的 prefill 合流。可验证预言:CSV 的 `prefill_side_census`
   应随 k 从 pim 翻向 gpu。
5. **"要区分两根轴:前几轮的驻留越大越走 PIM;当前 tier 的新段
   (reuse 以外)越大越走 GPU"**——chenyi9 的刻画正确,我此前
   "深 tier 更早翻 GPU"的表述有误导(那是固定 k 下深 tier 位移块
   多 → m 大在推,不是深度本身)。

**代码审计**(`src/workload_runner.py:2982-3024`,dynamic 估价):
`t_xpu` = 读回链路(字节 ∝ **驻留未修正行**)+ GPU attention
(m=compute_positions,宽维=全上下文);`t_bank` =
`ceil(m/cap)` 趟 ×(每趟实测扫描 ∝ 驻留 + TLB plan)+ m 比例
Q/ctx 链路;cap = min(batch, 512 B 容量 8)。结论:**与两轴刻画
相符**——驻留线性进两边、比值近似相消(固定 m 下深 tier 因摊薄
微弱偏 PIM);m(修正行+新段行,代码不区分两者,物理上等价)
线性抬 PIM 侧 sweep 数,越大越走 GPU;翻边阈值是行数量级
(m* ~ 百行,驻留无关)。审计判定:选边代码无须修改。

## 夜:下一步——头内序列切分(sequence split across HBMs;chenyi9 提出)

- **动机**:KV 头数 < HBM 数时 head→HBM 映射留下闲置堆叠。现状:
  num_hbm=5,LLAMA-7B/TP8 每设备 4 头 → 4 用 1 闲;GQA 模型
  (KV 头 8,TP8 → 每设备 1 个 KV 头)只用 1/5 HBM。chenyi9 设想:
  **一个头的 KV 按序列段切到 hbm_per_head = ceil(num_hbm /
  local_kv_heads) 个 HBM**(如远端池 16 HBM、8 KV 头 → 每头 2
  HBM),NVLink 字节不变而 PIM 扫描并行加倍 → A2/A6、A3a/A6 差距
  按比例拉大。
- **文献支持(已核)**:Flash-Decoding(2023,序列维切分 + LSE 合并,
  条件同构:并行单元 > batch×头数)、FlashDecoding++
  (arXiv:2311.01282)、Ring/Striped Attention(arXiv:2310.01889 /
  2311.09431)、Megatron 序列/上下文并行(arXiv:2205.05198;
  LLaMA-3 报告 arXiv:2407.21783)、DistAttention/Infinite-LLM
  (arXiv:2401.02669)、LoongServe(arXiv:2404.09526);KV 头=8 的
  现实依据:GQA(arXiv:2305.13245)。
- **引擎落点(设计草记,未实现)**:die 侧已有 `die_lse_merge`
  按物理 run 合并局部 softmax 元组——头内切段只是每头多一份局部
  元组;trace 侧为 stripe 规则的对偶推广(heads_per_hbm<1 的分支:
  L 先按 HBM 组分段、段内再按 channel 条带),wrapper 侧
  num_ops_per_hbm 与墙钟乘数相应改。**下一步要做,等 chenyi9 指令
  再动代码。**

## 夜 2:头内序列切分落地 + GQA 支持(裁决 chenyi9,"没错误就动手")

- **概念澄清(chenyi9)**:attacc system = GPU 部分 + PIM 部分;加的是
  **PIM 侧 HBM 数(= 远端 KV 存储)**,GPU 的 HBM 与 NVLink 不动。
  规则:KV 头独占 HBM;`kv 头数 < 堆叠数` 时每头
  `hbm_per_head = num_hbm // kv_heads_local` 个堆叠,K/V 按序列切段
  并发扫(除不尽的余数堆叠闲置);头多于堆叠退回 stripe 规则。
- **GQA**:模型表启用一直闲置的 `gqa_size` 字段,新档
  `LLAMA3-8B = [32, 4096, 32, 128, 3.5, 4]`(32 Q / 8 KV,组 4,
  FFN 14336;与 LLAMA-7B 同形对照)。单 KV 头 token 体积与 MHA 相同
  (512 B/token/层);组内 Q 头 = 同一 KV 头上的驻留查询
  (进 shared_queries/MQ 轴,与服务波宽相乘,容量 8 按组折算成员数)。
- **实现**:
  - wrapper 两条入口路径:`hbm_per_head` 因子,run 长度签名前
    `ceil(L/hbm_per_head)`(5-HBM ≥4 头时因子 1,v2 缓存键不变);
  - system.py 修复管道断点:Ramulator 构造原本**没传 num_hbm**
    (config NUM_HBM 到不了 wrapper,恒 5);
  - main.py `--num-hbm`(默认 5)→ `make_pim_config`;ladder 脚本
    `${NUM_HBM:+--num-hbm}`;
  - runner:PIM 扫描 op 的 `numOp` 改**本地 KV 头数**(GPU 侧 op 保持
    Q 头数),六处驻留查询数 ×gqa 组、sweep 成员容量 ÷组;
    `kv_row_bytes`(A2 远端流量)按 KV 头折减;落 KV 带宽乘
    `_hbm_seq_split`(切段头独占多堆叠并发);die LSE 合并宽度乘
    切段数(每段一份局部 softmax 元组,合并原语现成)。
- **验证**:41/41 单测绿(MHA@5HBM 行为逐位不变);wl_tiny 四组冒烟
  (7B 与 LLAMA3-8B 各 A4/A6 @ num_hbm=16)结果见当日记录尾注。
- **冒烟尾注**(wl_tiny @num_hbm=16,k=2,dag):LLAMA-7B A4/A6 =
  0.9637/0.9584 s(与 @5 HBM 几乎持平——wl_tiny 为 GPU 地板主导,
  扫描 ÷4 藏于地板下);LLAMA3-8B(GQA)A4/A6 = 1.0153/1.0118 s,
  链路字节与 7B 相同(Q 侧不变)、能量 137/134 J(组内驻留查询
  搬运)。两模型 A6 ≤ A4 成立,四组 exit 0。

## 夜 3:k 轴在 16-HBM 下的再分析(chenyi9 追问"现在 k 有影响了吧")

结论:**有,但走 prefill 选边轴,且方向与 A3 相反——A3a−A6 随 k
收窄,A3−A6 随 k 拉大,两条线在 k* 处交叉。**

- **decode 轴仍 k-平**:掩读下 A3a/A6 扫描字节同为 ctx×(1+k/256),
  同乘相消;序列切分把两档一起 ÷4(7B@16),中段 tier 的 A3a 扫描
  (~57÷4 ≈ 14 µs/层)也钻进 15.7 µs 的 GPU 地板 → decode 轴的
  A3a/A6 差距在 16 HBM 下反而压缩,只剩深 tier 残留(t9 ~×2.2,
  原 ×3.9 渐近比被地板截断)。
- **prefill 翻边点被 16 HBM 推进扫描范围正中**:选边两侧不对称变化
  ——PIM 路每趟全域扫 ÷4(~1.1 µs/层 @16k),GPU 路整段读回走
  NVLink **不变**(~55 µs/层)→ 阈值 m* 从 ~100 行升到 **~400 行**,
  除以位移块数(30–48)得 **k* ≈ 8–16**,正落在 {2,4,8,16,32} 中间:
  k≤8 A6 选 PIM(prefill 轴对 A3a 拉开 ×2–8),k≥16 翻 GPU
  (prefill 合流,只剩 decode 残留)。
- **GQA 档(LLAMA3-8B)k* 落回 ≈2–3**:读回按 KV 头折减(÷4)使
  GPU 路变便宜,PIM 每趟 ÷16 被"容量 8÷组 4 = 每趟 2 成员"抵掉
  一半;GQA 的 A3a/A6 prefill 区分只在 k=2。decode 比值与 MHA 相近
  (复制 ×4 与 sweep ×4 相消)。
- **可验证预言(更新)**:7B@16 套件 `prefill_side_census` 的 A6 列
  应 k≤8 以 pim 为主、k≥16 翻 gpu 为主;GQA 套件几乎全程 gpu
  (k=2 混合)。两条在跑的套件链可直接对数。

## 夜 4:两条提速杠杆落地(chenyi9:"两条都改,零结果影响")

- **诚实修正**:此前提议的"五个 k 共享一次构图"**不成立**——事件图
  依赖重算行位置(k 改 diff 池/掩集/断流结构),强行共享会改数值。
  该杠杆降级为"按 workload 成链调度"(A1 去重的无竞态载体),
  提速预期从 ×1.5–1.8 下修到 **×1.15–1.3**。
- **A1 去重(零数值影响,论证)**:ladder 里 A1 恒为
  `--reuse no-reuse` 且不带 `--epic-prefix-recompute-tokens`,其命令行
  在任何 EPIC_K 下**逐字相同**,输出确定 → k≠2 批跳过 A1、复制本链
  k=2 的 `dag_A1.json` 与重跑逐位等价。每套件省 20 次 A1(常为
  批内最重档),并每批省 ~40-50 GB 构图内存。
- **实现**:`run_dag_ladder.sh` 加 `SKIP_A1=1 A1_JSON=<path>`(缺文件
  即响亮失败,拷入 OUT 后照常 collect);`run_dag_suite.sh` 重构为
  **每 workload 一条链**(k=2 全档带暖 → k∈{4,8,16,32} 免暖去 A1,
  链内自知 k=2 输出路径,无跨批竞态),N_PAR 管链数,同一时刻仍
  至多 N_PAR 个批 → 核/RAM 包络不变;输出目录命名约定不变。
- 15:06 用新管线重启两条 16-HBM 链(L3-8B → 7B);重启损失 ~16 min
  构图。教训重演:pkill 模式含真实文件名(`bash -n ...sh`)再次
  自杀 shell(exit 144)——括号技巧要覆盖**整条命令行**里的每个
  出现,不只 pkill 参数本身。


## 夜 5:RTL 对簿——能量钳位入模,PC 默认开,全部重跑(裁决 chenyi9)

- **对簿结论(chenyi9:"看看是你算的对还是他算对了")**:RTL 侧对。
  `kvpim-rtl/docs/Fugue-asplos2027` 用与仿真器同源的能量常数证明:
  PC 的 6-tCK 地板本质是**每窗口能量预算**(E₆ = 140.8+5.23 pJ,
  n=1 命令顶红线),n=8 的 MQ 命令 E_cmd ≈ 176–183 pJ → 间隔必须
  ⌈6·E_cmd/E₆⌉ = **8 tCK**,对 ê 形状全程鲁棒(CSV 能量项 7.0–7.6);
  6 tCK 不可达,平衡点 PE 时钟 **f\* = 1/tCK = 1.3004 GHz**(每命令
  拍恰一次 MAC,ASAP7 可综合,+3.2% 面积)。仿真器现行 C 模型缺此
  项且 preset 1.733 被 ceil 罚成 7 tCK——双重偏差。**注意:这不是
  推翻 R1**(当年删掉的"等功率摊平"仍然是错的);窗口预算钳位是
  另一形态,且有综合证据。
- **落地(R19)**:`mq_interval_cycles` 增 PC 能量钳位(ê 保守 1.0,
  n=8→8、n=1→6、NPC 不动,六组关键值单测口算验证);A5/A6 preset
  1.733 → **1.3004 GHz**;`--powerlimit` 改 BooleanOptionalAction
  **默认开**(--no-powerlimit 显式关),config 侧默认同步;trace 生成
  器头部"MACAB: 8tCK"上游遗留注释改为"preset/override 治理";
  活文档五处(README 阶梯表、intro A5/A6、run_experiments、
  delta_vs_xinyao)同步 1.3004/8 tCK;测试断言 (1.733,512,8) →
  (1.3004,512,8)。
- **作废令**:此前所有 NPC/7-tCK 口径的输出(含今日 16-HBM 两条链
  的全部批次与冒烟)**作废**;PC 签名与 NPC 键天然不同,v2 缓存无需
  轮换,PC 从零暖。
- 全仓扫描:除五处活文档与上述代码外,无其余 1.733/6-tCK 残留
  (session/台账历史行保留原文,R16 行挂 R19 修订注)。
- **尾注**:PC 端到端冒烟通过(wl_tiny A6 @16 HBM,0.9585 s,exit 0;
  地板主导故与 NPC 持平属预期);16:45 以 PC 默认重启两条 16-HBM 链
  (L3-8B → 7B,N_PAR=2、W=6),driver 日志
  `output/suite_driver_20260827-164532_hbm16pc_*.log`。PC 同时把
  replicate 档列节拍 4 → 6 tCK(NPC → PC preset),A1–A4 会相应变慢
  ——这正是论文口径。


## 夜 6:docs/ 全量对账(chenyi9:"三档全部修掉")

- ⚠️ 旧 MQ 公式残留:intro/A5 行 27、delta_vs_xinyao §1 → R19 钳位版;
- ❌ 结构性缺口:README(硬件轴一节:R18 恒开、--num-hbm/序列切分、
  GQA 档、PC 默认;快速上手加套件链命令与 tiers CSV;索引补
  README_head_hbm_remap;缓存文件名改 v2)、README_run_experiments
  (链式套件+A1 去重、NUM_HBM/SKIP_A1 旋钮、GQA 模型档、tiers CSV、
  v2 缓存名)、delta_vs_xinyao(增 R18/序列切分/GQA/R19/逐 tier/
  链式套件六条)、intro/A3(物理孪生已补齐 + "以解析为准"反转 +
  R18 注)、intro/A1(状态节:DAG 默认/R18/PC);
- 🔴 真错误:intro/A4 行 16 "master_pool_channels 默认 8(ch0–7/
  ch8–15)"为 R5 前口径,**R5(08-24)即已改 15/1**,漏改至今——
  已改并标注旧稿性质;
- 未动:sessions/ 历史文件、archived/、README_experiments
  (paper_ladder 流程文档,待严格论文模式重启时单独对账)、
  README_head_hbm_remap 的序列切分推广小缺口(该文保持 R18 当时
  记录,推广见 delta §新增与本 session 夜 2)。

## 夜 7:560 GB 保卫战(运行时处置记录)

- 18:0x 哨兵连报:star+pipeline k=2 双链构图在 16-HBM/PC 口径下比预估
  重 ~30%(star 六档 ~315 GB、pipeline ~207 GB),合计奔 560 线;物理
  free 尚有 200 GB,内核不会替我们换出——560 是管理线不是物理线,
  冻结只能止增不能减驻留。**结论:此体量下两条大链装不进 560,
  必须独跑。**
- 处置序列:冻结 pipeline(止增)→ 确认仍会破线 → 冻结 driver 防
  补位 + 杀 pipeline 批(chain ABORT,533→424 GB)→ driver 冻结落地
  前抢跑起了 debate 链(18:28:06),趁其构图 2 分钟内损失≈0 立即
  拿掉(ABORT)→ **star 六档独跑(333 GB)**,driver(870669)与
  7B 套件包装(870668)双冻结。
- 后续计划:star 链(5 个 k 自持续跑,与冻结的 driver 无关)完成后
  SIGCONT driver → mapreduce+multisource 双链(体量较小,哨兵盯);
  L3-8B 套件收尾后**手工补跑 pipeline、debate 两条链**(逐条独跑);
  7B 套件不走冻结中的 N_PAR=2 包装,届时改为逐链手launch。
- 教训入档:N_PAR 的内存口径必须按**最重两条链的构图峰值之和**配,
  不是平均值;16-HBM/PC 的构图足迹比 5-HBM/NPC 时代重,560 GB 下
  大负载(star/pipeline/debate/multisource)只能独跑,可并行的只有
  "大+小"组合。

## 夜 8:k 轴降为三点 + 编排器接管(裁决 chenyi9)

- **裁决**:比例轴 {2,4,8,16,32} → **{2, 8, 32}**(每模型 25 批 →
  15 批;构图重复是当前主导成本,砍 k 是最大合法杠杆)。
- 落地:`run_dag_suite.sh` 链内 k-sweep 改 `for K in 8 32`;新增
  `SUITE_WORKLOADS` 子集覆盖(断点续跑用);README_run_experiments
  同步。
- **接管手术**:在跑的 star 链是旧脚本解析的五点循环,文件改动对其
  无效——摘除链壳(870674)与冻结的 driver/包装(870669/870668/
  870664),star k=2 的 ladder(870680)独立存活继续;
  `output/orchestrate_20260827.sh` 编排器接管全部剩余波次:
  等 star k=2 CSV → star k8/k32(SKIP_A1)→ L3-8B 两对
  (mapre+multi、pipe+debate,N_PAR=2)→ 7B(star 独跑 → 两对)。
  监视器换到编排器日志。
- **三点口径 ETA**(18:50 校准):star k=2 CSV 今晚 22:00–01:00;
  star 链完成明晨 02:00–06:00;L3-8B 全套 28 日晚–29 日凌晨;7B 三波
  29 日深夜–30 日中午;合计剩余 ~30–42 h(五点口径 ~55–70 h,砍两点
  省 ~10 批构图 ≈ 20–25 h)。编排器脚本在
  `output/orchestrate_20260827.sh`(运行产物区,不入库;逻辑全文见
  本节),日志 `output/orchestrator_20260827.log`。

## 夜 9:构图二次方事故修复(chenyi9:"修,验证零差异")

- **诊断**(py-spy 三连采样,全部命中同一处):
  `_annotate_batch_q_arrivals` 对每个 decode batch 全量线性扫两遍全部
  事件(找 Q 到达 + 找 sweep)→ **O(batches × events)**;star r5 规模
  ≈ 1.8 万 × 数百万 ≈ 5×10¹⁰ 次判断,单处 ~20–40 min,k=2 批因 warm
  焚烧遍 ×2。wl_tiny 规模无感,套件规模爆炸——"构图 35 min → 2 h+"
  的主因,不是泛泛的"Python 慢"。
- **修复**:预建两个索引(O(E) 一遍)——Q 事件按 (层, 请求) 分桶、
  sweep 事件按 batch id 分桶;桶存 (扫描下标, 事件),跨成员合并后按
  下标排序,**输出列表与旧实现逐字节同序**。函数从几十分钟降到秒级。
- **零差异验证**:41/41 单测绿(套件本身快 15 s);wl_tiny A4/A6
  @16 HBM/PC 新旧代码报告**全键逐值相同**(batches/时序/能耗/逐 tier
  源数据;唯一排除的 `ramulator_signature_cache` 统计键差异为纯缓存
  状态,A6 连它都全同)。
- 生效方式:在跑的 star k=2(旧代码已载入)不动;编排器后续 28 个批
  均为新进程,自动吃到修复——每批构图预计 ~2 h → ~1–1.2 h,全程再省
  ~8–15 h。

## 夜 10(实为 08-28 凌晨):W1–W4 提速落地 + 三路对抗审查(裁决 chenyi9)

- **W1+W2(合并实现,build-once warm)**:`_run_cacheblend_prefill` 切出
  `_finalize_cacheblend_report`(校验/调度/标注/报告),加 `_build_sink`
  提前返回;新 `_warm_build_price_finalize`:占位价建图一次 → 台账记
  (op, 事件下标)三类("runs"/“agg”/“agg_cb”,能量口径各按创建处镜像)
  → 并行仿真 → 缓存命中重定价 `dataclasses.replace` → 唯一一次
  finalize。A6 估价探针经 `_WARM_BYPASS` 走真实入口(决策不见占位价)。
  wl_tiny 暖遍 44 s(旧流程分钟级 ×2 遍);k=2 批省一整次构图。
- **W3**:`_RETAIN_EVENT_ADDRESSES` 门控(events=none 时不驻留地址
  数组,验证器同门控);诚实修正:地址本已是紧凑 array("Q"),收益从
  预估"数十 GB"降为小头——按实际价值记录。
- **W4**:套件 `K_PAR`(链内 k-sweep 并行)能力落地、默认关;审计
  结论:560 GB 下大负载无安全并行空位,能力供放宽内存后用。
- **验证**:41/41 × 3 轮;wl_tiny @16HBM/PC 暖模式 A1/A3/A4/A6 + 免暖
  A4 全部与 HEAD 基线**逐值相同**(排除项仅缓存统计元数据块)。过程
  中抓获并修复一处真缺陷:A1 的 bank-whole 聚合扫描(2419 行)未入
  台账 → 重定价漏项(首轮 A1 diff 暴露,加第三类 "agg_cb" 后同一)。
- **三路对抗审查(多 agent 交叉验证,chenyi9 指令)**:
  ①W2 完备性:PASS——Acc 计价点全枚举、下标账目闭环、生命周期
  finally 清理;确认项:缓存统计块在 A6 判 gpu 时可差(属排除项)、
  `_RETAIN_EVENT_ADDRESSES` 粘滞(已加固:A2 路径同置位)、warm 路径
  无单测(记为后续项)。②W3 门控:PASS——消费方清点(to_dict/验证器/
  一处测试)、跨路径串跑探针字节同一。③重构+K_PAR:PASS——移出尾块
  逐字符同一 + AST 自由变量审计 22 名全覆盖;**Stage-B 准入排序的
  时序无关性获独立证明**(sort key 全为正时长 LINK 事件,同资源完成
  序单调于追加序)并已加防退化注释;bash 层(local 数组/PIPESTATUS/
  并发 cp)无缺陷。
- 编排器 v2(`output/orchestrate_v2_20260828.sh`):star 链改为新引擎
  直跑(k=2 起),后续波次同 v1。

## 终注(chenyi9 裁决,08-27 晚):本版本冻结

- **822-dirty 自此冻结**:编排器 v2 照跑(L3-8B star 链 20:05 起 →
  两波 → 7B 三波),不再改动本分支代码;
- 后续提速路线转移到新实验分支(基于本分支新建):**核心代码 C/C++
  重构**,先在 wl_tiny 上对数、再复跑 experiment 1(= 本分支在跑的
  star @16HBM/PC 三点链)。

## 分支线(chenyi-822-cppcore-exp,自 1a231b6;chenyi9 裁决 08-27 晚)

- 任务:822-dirty 冻结自跑;xinyao 工作区核对无未提交内容(仅一个
  未跟踪本地备份目录)后拉到 chenyi-822-dirty,再基于它新建本实验
  分支,把核心重构为 C/C++,并复跑 experiment 1(= dirty 在跑的
  star @16HBM/PC 三点链)。
- **v1 落地:原生调度核**(`src/cppcore/eventcore.cpp` + ctypes 桥
  `src/cpp_eventcore.py`):事件创建时镜像 (device, duration, deps)
  进核,增量准入排序与全图调度原生化——顺带消灭了增量调度器每轮
  整拷 finish 字典的**第三个隐性二次方**;浮点同序同运算,逐位一致;
  `KVPIM_CPPCORE=0` 纯 Python 回退完整保留,overlap 校验器每次运行
  用 Python 重放核对原生调度(内嵌交叉验证)。
- 基建:ramulator2 二进制从 822 同步(8-21 本地版已因 HBM3-PIM 时序
  改动过期,md5 证实)、v2 签名缓存快照拷入。
- 验证:41/41(核开启);wl_tiny A4/A6 三方逐值同一(C++ = 本分支
  Python = 822 冻结基线);墙钟 −21%(48.2→38.1 / 51.0→40.0 s)。
- experiment-1 复跑:**排队在 822 编排器 ORCHESTRATION DONE 之后**
  自动发射(560 GB 纪律,两边不并跑);路线图 v2/v3(构图主体、
  warm 台账下沉)见 `docs/README_cppcore_branch.md`。

## 分支线夜 2:experiment 1 全量搬上 cppcore(裁决 chenyi9,08-27 晚)

- **资源裁决**:"Python 版最多 100 GB,cpp 版 500 GB 现在就跑" →
  822 冻结分支的 star k=2 六档 SIGSTOP 冻结(43 min 构图进度、64 GB
  驻留,随时可续/可弃);cppcore 的 exp1 star 链 20:49 直接发射
  (不再等 822 收官;缓存快照同步,star 形状冷、自付暖仿真)。
- **中期 A/B(RSS 进度代理)**:同一张图同一顺序,内存即进度刻度——
  cpp 11 min 到 41 GB,Python 同进度需 ~19–20 min → **构图 ~×1.8**,
  且 Python 侧被消灭的字典整拷二次方在后半程才发作,比值预计继续
  拉大;`[warm]` 落地给终值。
- **"1" 裁决**:cpp 限额放 ~600 GB → 21:07 接力链发射
  (mapreduce → multisource → pipeline → debate,每档 3 worker,
  与 star 暖峰合计 ≤90 核);哨兵改 640 告警。
- **exp1 全量定义澄清(chenyi9)**:= **两个模型 × 五个 workload
  全部在 cppcore 跑** → 7B 三波等待器已挂(L3-8B 双跑者收官自动
  触发:star 独跑 → mapre+multi → pipe+debate),终点标记
  `EXP1 ALL MODELS DONE`。
- ETA:L3-8B 五负载 ~28 日上午齐;7B 随后 ~12–16 h;
  **exp1 全量 ≈ 28 日夜里**。822 冻结侧数据职能被 cppcore 全面接管,
  仅存构图 Python 基线价值,处置等裁决。

## 分支线夜 3:放置语义终局(裁决 chenyi9,08-27 深夜)

- **两次校正合一的终局**:①baseline(A1/legacy 路)= AttAcc 原文
  "一头一 channel"(该头全部 token 在自己 channel 内:单元 u=t mod 16、
  bank 存 dhead 四分、row=t÷256、列=2·((t mod 256)÷16));②复用档
  pool run = **我们的布局**:软件 map 按 256-token chunk 选 channel,
  同用 chunk 异 channel——wrapper 仅对带 channel_base 的 run 传
  stripe 旗标,每 channel 只扫自己 chunk 份额;签名对 pool 键追加
  `chunkstripe1` 独立键空间(防复用 v1 幻影条目),legacy 键与 v1
  兼容(761k 行老缓存对 A1 类直接可用)。R18 全条带与序列切分撤销
  (跨堆叠切头违背 softmax 局域性)。
- **拓扑定稿**:1 GPU : **16** 个远端 HBM-PIM 堆叠(原配 5 = 镜像
  A100,设计选择非物理必然;"8"是 num_attacc 设备数)。堆叠间无直连
  (星形挂设备级 logic die);softmax 在**每堆叠底 die**(AttAcc 原文
  位置)⇒ **一 KV 头一 HBM,头少堆叠闲置为接受的余量**;TLB per-HBM
  (现 16-channel 模型即一个堆叠的 TLB,无需全局池重构)。落 KV 带宽
  = per-HBM 份额(池=单头单堆叠)。
- **验证**:trace 双探针(legacy 64 MAC@ch0;pool 15ch 每 channel 恰
  64 条);41/41;wl_tiny @16HBM/PC A4 0.9651 s/115.7 J、
  A6 0.9596 s/114.4 J(A6≤A4)。**stripe 口径与幻影口径数据全部作废;
  exp1(两模型 × 五负载,cppcore)按终局语义重启。**

## 分支线夜 4:experiment 1 的有效资源配比记录(chenyi9 指令)

- 名义拓扑 1 A100 : 16 远端 PIM 堆叠(系统 8 × 16 = 128 堆叠),但
  "一 KV 头一堆叠 + 头少闲置"下,**实际参与计算的配比**:
  - **LLAMA-7B(MHA,TP8)**:每设备 4 个本地头 → 4 个堆叠在算,
    **等效 1 A100 : 4 PIM**(系统 32/128 堆叠活跃);
  - **LLAMA3-8B(GQA,TP8)**:每设备 1 个本地 KV 头 → 1 个堆叠在算,
    **等效 1 A100 : 1 PIM**(系统 8/128 活跃);
  - 其余堆叠 = 容量/隔离余量(裁决"16 个可能用不完"),不贡献带宽。
- 论文口径提示:资源账(能耗、面积、perf-per-stack)按**活跃堆叠数**
  报才诚实;16 是容量配置,4/1 是算力配比——exp1 的全部数字都在这个
  等效配比下产生。
- **修正(chenyi9 算账指令)**:上文"16 是容量配置"不成立——单头
  全 32 层每 token 16 KB,一个 16 GB 堆叠可驻 ~105 万 token,我们最大
  上下文 40.7k 仅其 3.9%(全负载驻留峰 ~15%)→ 闲置堆叠**连容量都不
  贡献**。**等效配比(算力与容量双口径)= 7B 1:4、8B-GQA 1:1**;
  16 只在单头驻留 >1M token 或多租户隔离时才有意义,均不在 exp1
  范围。8-MiB/bank K 窗口仍是连续布局的首个约束。
