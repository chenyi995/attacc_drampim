# 2026-08-25:审计日——五题裁决、治理规则、workload 有效性(chenyi9)

## 阶梯审计与修复(台账 R4–R8)

- 工作法:先写 audit 文档(现象/代码/**归因到 attacc/xinyao/chenyi +
  commit**)、我裁决后才动手;修复与未修复分列在**一个**台账文件里;
- 五题裁决:A6 估价与入账同口径(修);A5 不是优势档,是"强制全上
  PIM"的对照档,A6 选完报比例即可,不造交叉点专门设计点(口径);
  分池改 **15/1**——diff 行极少,不许对半分掉 master 带宽(修);
  naive 布局补 channel 冲突:逐 chunk 顺序分配并追踪、同 channel
  串行化(修);A5/A6 绑定平衡点 **2.6 GHz / 768 B**(PROVISIONAL,
  之后还会调)(修);修后全矩阵重跑(145/145);
- memory report 属主副本双计修复 + 存量结果算术修补;多轮历史口径:
  名义历史应**一轮轮从 0 累积**(mooncake conversation 转真多轮);
- 旧 run 全删,只保留新代码重跑的;软件库单独 external/ 只留链接;
- 撤回"选择变体 sweep":模拟器只认重算 token **数量**,"上游软件
  本来就不管,不要自作主张"。

## 治理规则

- **称呼两维度**:回答用服务器账户名(`whoami` 现取);文件里的决策
  记录写**决策账户名**(本账户 chenyi9),任何文件不得出现真名;
- **git 两级闸门**:没有明确指令**不 commit**;commit 指令**不隐含
  push**;过往指令不延续。

## workload 有效性(台账附录 B 的来源)

- 发现:Mooncake 三个 trace 的哈希是**严格前缀链**,构造上无位置无关
  复用,重算机制从未被真正检验;
- 判据收紧:可用 ⟺ **真实 workload 的数据自身**产生内容位移复用;
  规则强制重算与合成设计品都不算;**只有可用/不可用两档,没有
  "半可用"**;
- 判定:multihop 唯一可用(gold evidence 当检索结果,模拟器层面合理
  ——chenyi9 确认);relay(手写 Δ)/sharegpt(唯一位移=3 token 重复句)
  /mooncake/mooncakemt 不可用;tokenizer 代理口径记录在案;
- 方向:先列 workload 搜索标准供审阅,确认后再找(次日执行)。
