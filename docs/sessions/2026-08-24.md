# 2026-08-24:A1–A6 阶梯重定义(chenyi9 裁决记录)

- **废除旧 A6 "split 混合 prefill"**(GPU 算新行、PIM 扫旧行、LSE 缝合,
  xinyao `0aced82` 引入):现有结构要么 GPU 要么 PIM,不存在两边各算
  一半;
- **A1–A6 重定义**:A1 AttAcc 无复用(参照)/ A2 纯软件复用 /
  A3 +乱序布局 / A4 +channel 拆分 / A5 所有 prefill attention 上 PIM
  (**attention batching 与之同步启用**,一个包)/ A6 = A5 + 逐请求
  动态放置(= C3,即 Fugue);A1–A6 默认多轮 agentic 编排;
- **物理事件 DAG 与 A 阶梯同菜单**(gpu/pim/dynamic):一个编排,直接
  跑也可以单独跑,与 A 系列行为一致;
- **分支策略**:新建 `chenyi-822-dirty` 快速集成、先跑通;干净分支
  `chenyi-822` 之后按逐步审阅移植;
- **文档体系**:docs 总 README(面向不了解本项目的读者)+ 每档
  README_Ax(每步在哪里做)+ delta-vs-xinyao0821 全量差异页;
- **论文证据矩阵**:同一批 workload × A1–A6 × LLAMA-7B/65B/GPT-175B;
  指标 TTFT/TBT/压缩率 + A6 的 PIM 选边比例;64 核并行(当日稍后提到
  32 并发驱动);复用策略只取**保证重算**一族(cacheblend 同族;EPIC
  是组内"取前 k 个 token"的特例),零重算的 promptcache 不进矩阵主
  口径;
- 工作方式:关键代码注释要结合论文逻辑,让审稿人能对上。
