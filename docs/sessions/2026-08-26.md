# 2026-08-26:引擎全覆盖与运行基建日(chenyi9 裁决记录)

## workload 标准修订与 14 源体检(标准文件在
`/data2/chenyi9/KV-PIM/workload/README.md`)

- B2 修订:纯 multi-turn **不一定有复用**,优先找 **multi-turn agent**
  场景并逐源重点核查;E1 放宽:128 核 2–3 小时或 64 核一天以内均可;
- **C0 无 GPU 原则**:CacheBlend 格式的复用是**编排层复用**——"哪一段
  复用什么"由编排/数据决定;复用软件真跑只决定"哪几个 token 被重算"、
  **不决定"哪里被复用"**,具体重算哪几个 token 需要时**按比例或按个数
  随机抽取**即可 → 无须真跑复用软件,workload 制备全程无 GPU(相应
  CacheBlend 调研任务停掉;822 审计文件 W3 同步作废改写);
- D2:token 化必须用**真实模型 tokenizer**(cl100k 代理淘汰);
  F:模型档 MHA 与 GQA **都要**,GQA 收益小就照实报小;
- GPU/联网事项单独交接文件 `README_gpu_tasks.md`(整文件夹拷到 GPU
  服务器由那边 AI 执行;G1 接力输出生成待激活、G2 gated tokenizer
  代取);
- staging 区定位:`/data2/chenyi9/KV-PIM/workload/` **只是真实负载源
  数据暂存区**;不可用源全删;初筛通过的 13 个新源全部下载,
  **每源一个文件夹**出统一口径体检表(BODY_CHECK.md)。

## 引擎与运行

- **真实 workload 默认走 DAG 事件依赖路线,不能纯走解析模型出数**;
- 问"为什么 DAG ≈ A4/5/6"之后下令:**修复 DAG 覆盖 A1–A6**
  (`--engine dag`;台账 R11);
- **Ramulator 先用最多 64 核把 cache 建好再跑**(签名缓存落盘,
  台账 R13;当日两次现场诊断:decode 串行调用 → 预热;py-spy 定位
  聚合入口漏拦 → 双入口预热+形状去重);
- **链路字节定义**:统计的是 GPU 与**远端存储**(HBM 或老 DRAM)之间
  经 NVLink/PCIe 的链路,不是"GPU 和 PIM";裁决 **A2 的 KV 放远端
  哑存储**(台账 R10;解析引擎旧口径遗留 U3);
- 归边统计纠错(chenyi9 发现 A6 分母变 2):统一分母=全部请求,
  普查 pim/gpu/mixed/none + 行加权 share(台账 R12);none = 整层复用
  零修正、prefill attention 未发生;
- **六档全并行跑**(run_dag_ladder.sh 由串行改并行发射)。

## 目录与文档

- 自造 workload 放 `<repo>/workload/`;输出放
  `<repo>/output/<时间戳>_<负载>_<模型>/`;
- docs 重组:`intro/` 收 A1–A6,`archived/` 收弃用件;**几份 audit
  合并为唯一台账**(R1–R13 / U1–U6 + 流程裁决表 + 两个附录),
  workload 有效性审计归档;TLB 能耗常数与时序 overlap 分析补入 U1
  (只分析未修改);delta 与总 README 按现状刷新;分步 commit+push
  (`4e582cb`/`8b58fe7`/`c8e39c9`/`6fe7e71`);
- 新建本 sessions/ 目录按天记录调整。

## fig:motiv(论文 F1)与典型拓扑

- 用**有文献依据的理论负载**做 e2e 三柱:问题①搬运 memory-bound(A2)
  /问题②乱序访存(A3)/我们的方法(A6);星型场景 = 1 个 main agent
  指挥 3 个 worker 多轮往返(AutoGen/MetaGPT/AgentCoder 引用);
  论文仓库不动、`\TBDnum` 占位保持;
- 其余典型拓扑**先文献、再构造、再跑**:流水线(MetaGPT/ChatDev)、
  辩论(Du et al./MoA)、map-reduce 薄共享对照(Wu et al./LangChain);
  生成器均带 k/L 旋钮,F1 完整版两段扫描待定点后可出。
