# Independent reproduction check

The exported repository was extracted into a directory named `renamed-checkout` without `.git`, `output`, `ramulator2`, or `ramulator.out`. Only the included source, pinned dependencies and fixed workload inputs were available to the experiment code. Python dependencies and GCC 14 were already installed on the validation host.

- `python3 -m fugue doctor`: passed, including the C++20 probe and workload integrity.
- `python3 -m fugue all --jobs 8`: passed; fresh build, all five experiments, all figures and numeric verification completed in 128.21 s on this host. Runtime on another host may differ.
- 639 fresh native profile records; 344 rows in the main tables, 11,208 main-table numeric fields checked. Maximum relative difference from the prior final results: 2.63e-16, from floating-point expression ordering; CacheBlend and EPIC main numbers matched exactly.
- Independently checked all 50 final CSV tables, including derived tables, event/transfer/storage/energy rows and native transition evidence: 157,136 numeric fields passed.
- All 13 recreated PNGs were byte-identical to the checked-in final figures; the explicit redraw path produced identical PNGs too.
- `all --resume` skipped the completed unchanged stages and passed. The redraw command completed independently without using fresh scan inputs.
- Executable source, simulator source, input and dependency hashes in the released checkout match those in the successfully tested export.

[Machine-readable report](Fugue-asplos-reproduction-checks.json) records commands, exact results and the source/input identity. Full clean-run traces and logs are retained locally under `output/Fugue-asplos-portability-validation/`; they are regenerated by the supported command and are not required in a clone.
